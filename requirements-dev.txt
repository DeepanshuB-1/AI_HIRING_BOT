# Development / test dependencies.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
#
# The test suite mocks Twilio, Ollama, Redis, Celery, SendGrid and the database,
# so none of these pull in external services.

-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
